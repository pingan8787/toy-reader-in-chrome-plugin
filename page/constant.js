// 默认的模式
const DefaultMode = "default";

// 默认样式的 ID 名
const DefaultStyleFlag = "CHROME_PLUGIN_MP_WEIXIN";

// 默认暗黑模式样式的 ID 名
const DefaultDarkStyleFlag = "CHROME_PLUGIN_MP_WEIXIN_DARK";

// 默认暗黑模式样式的 Storage 的 key 值
const IsDarkModeKey = "IS_DARK_MODE";

const Constant = {
    DefaultMode,
    DefaultStyleFlag,
    DefaultDarkStyleFlag,
    IsDarkModeKey
}